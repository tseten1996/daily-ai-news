# Daily Improvements Log

Append-only. One entry per run of the Continuous Staff Engineer &
Architecture Review Agent.

## 2026-07-17
- Task: Bootstrap the `docs/` knowledge base (this file and its siblings) — no code changes.
- Category: Documentation (bootstrap run, per operating instructions)
- Why: The `docs/` knowledge base did not exist. Per the bootstrap protocol, the entire run is scoped to creating it accurately, with no code changes, so future runs have real state to read instead of starting cold.
- Files: `docs/architecture/overview.md`, `docs/decisions/README.md`, `docs/roadmap/roadmap.md`, `docs/technical-debt/backlog.md`, `docs/notes/daily-improvements.md`, `docs/performance/findings.md`, `docs/accessibility/audit.md`, `docs/seo/checklist.md`, `docs/testing/strategy.md`
- Verification: No build/lint/test pipeline exists in this repo (confirmed by audit — no `package.json`, no CI workflows, no test runner). Nothing to run; no code was touched to verify.
- Docs updated: all of the above (all newly created)
- Follow-ups: Backlog seeded with 8 open items across SEO (real meta tags/OG/Twitter/canonical, robots.txt + sitemap.xml, RSS feed), accessibility (heading hierarchy skip, no automated a11y testing), and architecture health (no build/lint/CI/test tooling at all, CSS/JS duplication across pages — flagged as an intentional tension pending an ADR, not a bug). Roadmap's "Immediate" section proposes real SEO meta tags as the first non-bootstrap task: mechanical, low-risk, no tooling-decision blocker.

## 2026-07-17 (same-day follow-up)
- Task: Scaffolded an Astro-based target architecture for the Articles stream and recorded the decision as an ADR, at the user's explicit request to start moving toward a componentized "SPA-route" architecture using best practices.
- Category: Architecture health (this is the ADR the previous entry's backlog item 6 said tooling changes were blocked on)
- Why: User-directed, not autonomously selected — but scoped conservatively per the agent's own safety rules: isolated under `site/`, not wired into deploy, legacy pages completely untouched, verified with a real build before committing.
- Files: `docs/decisions/ADR-0001-adopt-astro-for-spa-migration.md`, `docs/architecture/overview.md`, `docs/roadmap/roadmap.md`, `docs/technical-debt/backlog.md`, `docs/seo/checklist.md`, `docs/testing/strategy.md`, `SCHEDULED_TASK_PROMPT.md`, and a full new `site/` Astro project (package.json, astro.config.mjs, tsconfig.json, content collection schema, `BaseLayout`/`ArticleLayout`, `CodeTabs`/`Figure`/`Judgment`/`FailureBlock` components, one working example article, `site/README.md`).
- Verification: `npm install`, `npm run build`, and `npx astro check` all pass clean (0 errors/warnings/hints). Generated `dist/articles/example-post/index.html` inspected directly — confirmed correct meta description, canonical, OG, Twitter, and JSON-LD tags. Rendered in a headless browser via Playwright: zero console errors; code-tab switching confirmed working via before/after screenshot. The live legacy site (`index.html`, `articles/*.html`, `manual/*.html`, `.github/workflows/deploy-pages.yml`) was not touched.
- Docs updated: all files listed above.
- Follow-ups: 3 new backlog items opened — wire `site/` into CI/deploy; decide the fate of the 3 already-published legacy articles (duplicate-content risk); migrate the pilot's first real (non-template) article once those two are resolved. `SCHEDULED_TASK_PROMPT.md` explicitly instructs future runs to keep authoring new articles in the legacy format until deploy wiring is confirmed live, to avoid shipping content that doesn't appear anywhere.

## 2026-07-18
- Task: Added `robots.txt` and a hand-maintained `sitemap.xml` at the repo root.
- Category: SEO (priority 5)
- Why: The site started at zero SEO tooling with no build pipeline; between the last run and this one the repo gained a real `GitHub Pages` deploy workflow (`.github/workflows/deploy-pages.yml`, merged via PR #5/#6), so the site is now actually live and crawlable — making the total absence of a sitemap/robots policy (backlog #2) a live gap rather than a theoretical one. It's small (2 new files), mechanical, fully completable in one run, and unblocks nothing risky. Chose it over the larger per-page meta-tag task (backlog #1, ~13 files across index/articles/manual) which is better sliced across 2+ runs to stay within the file-count guideline; that stays queued as the new top "Immediate" item.
- Files: `robots.txt` (new), `sitemap.xml` (new)
- Verification: No build/lint/test pipeline exists in this repo (confirmed unchanged from bootstrap audit — still no `package.json`, and the one CI workflow only deploys, it doesn't test). Manually verified `sitemap.xml` is well-formed XML (`python3 -c "import xml.dom.minidom as m; m.parse('sitemap.xml')"` — passed) and cross-checked it lists exactly the 13 currently-published HTML pages (confirmed via `find . -name "*.html"` and cross-referencing `manual/index.html`'s published/queued module markers, so unpublished modules 06-15 are correctly excluded). `robots.txt` syntax matches the standard format and its `Sitemap:` line points at the correct absolute URL for this repo's default GitHub Pages URL (no `CNAME` file present, so `https://tseten1996.github.io/daily-ai-news/`).
- Docs updated: `docs/seo/checklist.md` (marked sitemap/robots done, added a maintenance note that future runs must add a `<url>` entry when publishing new pages), `docs/technical-debt/backlog.md` (#2 marked DONE; #6 updated to reflect the new deploy-only CI workflow and note that verify-only CI wouldn't violate the no-build-step principle — sharpens the pending tooling ADR), `docs/roadmap/roadmap.md` (removed the now-done item, reprioritized real meta tags as the sole "Immediate" item with a slicing note), `docs/architecture/overview.md` (corrected stale "no deploy workflow found" / "2 articles published" statements — the repo moved since the 2026-07-17 bootstrap review).
- Follow-ups: (1) Real per-page SEO meta tags (description/canonical/OG/Twitter) — now the top Immediate roadmap item, likely needs 2 runs to cover all 13 pages within the file-count guideline. (2) Sitemap is hand-maintained — every future run that adds/removes a page must update it, flagged in both the SEO checklist and backlog. (3) No new backlog items created beyond what already existed.

## 2026-07-19
- Task: Added the 2026-07-19 daily run's new article (`articles/no-framework-200-line-harness.html`) to `sitemap.xml`, which had been published live and linked from `articles/index.html` but was missing from the sitemap; also added a "finish every run" step to `SCHEDULED_TASK_PROMPT.md` so the daily content-authoring run keeps `sitemap.xml` current itself instead of relying on this agent to catch the gap after the fact.
- Category: SEO (priority 5)
- Why: This branch (`claude/stoic-allen-65bm3u`) had fallen behind `main` — its prior commits (an Astro pilot, PR #6) had been merged into a stale non-default branch rather than `main`, so it was restarted from `origin/main` before orienting (see follow-up below). Once oriented, the highest-priority, safely-completable gap was a live SEO regression: today's new article page is reachable and linked but invisible to sitemap-driven crawlers — exactly the maintenance risk the 2026-07-18 run had already flagged in the SEO checklist. Small (1 content file + 1 spec-doc line), mechanical, zero risk to the build (there is none) or to published content.
- Files: `sitemap.xml`, `SCHEDULED_TASK_PROMPT.md`
- Verification: No build/lint/test pipeline exists in this repo (confirmed unchanged). Verified `sitemap.xml` is well-formed XML (`python3 -c "import xml.dom.minidom as m; m.parse('sitemap.xml')"` — passed) and cross-checked its 14 `<url>` entries 1:1 against `find . -name "*.html"` (14 published HTML files, all accounted for).
- Docs updated: `docs/architecture/overview.md` (article count 4→5, review date), `docs/seo/checklist.md` (documented that the flagged sitemap-staleness risk actually occurred, and how it's now prevented), `docs/technical-debt/backlog.md` (#2 follow-up marked fixed; new item #10 opened), `docs/roadmap/roadmap.md` (added #10 to "Next").
- Follow-ups: New backlog item #10 (OPEN) — discovered while orienting that PR #6's Astro pilot / ADR-0001 (SPA migration decision) was merged into a stale branch (`claude/daily-ai-trends-tutorial-txft7z`) instead of `main`, so it's absent from and undocumented on `main` today. Not fixed this run — needs a deliberate revive-or-formally-shelve decision, not a mechanical sync. Full detail in the backlog entry and roadmap "Next" section.

## 2026-07-21
- Task: Reconciled the orphaned Astro pilot (ADR-0001, `site/`) into `main`'s current history, resolving backlog item #10 (open since 2026-07-19).
- Category: Architecture health / documentation (priority 7/10) — a real-state fix, not just a doc update
- Why: Orienting on this run's designated branch (`claude/stoic-allen-kf1dna`) found it was the branch that still carried the Astro pilot commit (`281cff4`, from PR #6's true content) forward, unmerged into `main` and 6 PRs behind it. The 2026-07-19 run had already diagnosed this exact split — a different branch that day had been restarted fresh from `main`, leaving the pilot's commit stranded on this branch rather than reviving it. Reviving it (merge, not discard) was the safer and more valuable choice: `git show` confirmed the Astro pilot's commit content (`site/`, ADR-0001) did not exist anywhere on `main`, so merging was the only way to stop losing it, and the alternative (formally shelving) would have thrown away a working, previously-verified pilot for no reason. This is exactly backlog #10's "reconciliation" path (option a), selected over "formally shelve" (option b) because the pilot's own verification (build + type-check) still passed clean after the merge — nothing about it had gone stale or wrong.
- Files: merged `origin/main` into this branch via `git merge`, hand-resolving conflicts in `docs/architecture/overview.md`, `docs/roadmap/roadmap.md`, `docs/seo/checklist.md`, `docs/technical-debt/backlog.md`, and `docs/notes/daily-improvements.md` (this file) to combine both branches' independent updates (the Astro-pilot decision + article/robots.txt/sitemap updates that had landed on `main` since) into one consistent narrative with sequential item numbering. No non-doc conflicts (the code-level changes on each side touched disjoint files).
- Verification: `site/`'s build and type-check were re-run after the merge to confirm the pilot survived intact — `npm install && npm run build` (2 pages built, 0 errors) and `npx astro check` (0 errors, 0 warnings, 0 hints), both clean. `sitemap.xml` re-validated as well-formed XML and cross-checked against `find . -name "*.html"` (16 legacy pages, 16 sitemap entries, still 1:1). No other build/lint/test pipeline exists in this repo for the legacy pages (unchanged from prior audits).
- Docs updated: `docs/architecture/overview.md` (merged current-vs-target state, corrected article/module counts to 7/6), `docs/roadmap/roadmap.md` (merged Immediate/Next sections, added "wire `site/` into CI/deploy, or decide not to" as a live Immediate item now that the pilot is visible on `main` again), `docs/technical-debt/backlog.md` (renumbered items 1-13 into one consistent sequence; item 10/13 marked RESOLVED with the reconciliation explained; items 8 and 9 — CI wiring and legacy-article fate — carried forward as still-OPEN, since reconciling history didn't answer either), `docs/seo/checklist.md` (merged review-date and page-count updates).
- Follow-ups: Backlog items #8 (wire `site/` into CI/deploy, or decide not to) and #9 (fate of the 7 published legacy articles vs. a future Astro migration) are the two real decisions this reconciliation surfaces but does not make — both now sit at the top of the roadmap's "Immediate" section for a future run or the repo owner to resolve deliberately. No new backlog items opened; this run closed one and reactivated two that were already known.

## 2026-07-22
- Task: Fixed a recurring missing-sitemap-entry regression (today's new article, `articles/tool-descriptions-are-prompts.html`, shipped without a `sitemap.xml` entry) and added a verify-only CI check (`scripts/check-sitemap.sh` + `.github/workflows/check-sitemap.yml`) so this class of gap is caught automatically instead of depending on a human or agent noticing.
- Category: SEO (priority 5), with a testing-gap angle (priority 8) — the fix is a code change (a CI check), not just a doc update
- Why: This branch (`claude/stoic-allen-6srxe0`) had no prior commits of its own and no open PR, so per the "already-merged designated branch" protocol it was restarted fresh from `origin/main` before orienting. Orienting surfaced that the exact SEO gap the 2026-07-19 run had fixed and specifically added a `SCHEDULED_TASK_PROMPT.md` reminder to prevent had recurred — today's article (committed the same morning) was missing from the sitemap despite that reminder already being in place. A second recurrence of an already-diagnosed, already-"fixed" issue is strong evidence the prose-reminder fix was insufficient, making the highest-value safe task today not just re-fixing the symptom again, but closing the actual gap: a structural, CI-enforced check. This is a minimal, additive, verify-only change (new script + new independent GitHub Actions workflow) that cannot affect what `deploy-pages.yml` ships, so it carries essentially no production risk while directly preventing recurrence #3.
- Files: `sitemap.xml` (added the missing entry, bumped `articles/` index `lastmod`), `scripts/check-sitemap.sh` (new), `.github/workflows/check-sitemap.yml` (new), `docs/technical-debt/backlog.md`, `docs/seo/checklist.md`, `docs/testing/strategy.md`, `docs/roadmap/roadmap.md`, `docs/architecture/overview.md`
- Verification: No pre-existing build/lint/test pipeline for the legacy pages (unchanged). Confirmed `sitemap.xml` is well-formed XML (`python3 -c "import xml.dom.minidom as m; m.parse('sitemap.xml')"`). Ran `scripts/check-sitemap.sh` against the working tree post-fix — passes (all 17 pages present). Ran it again against the pre-fix commit via `git stash` — fails with the exact expected error (`MISSING from sitemap.xml: articles/tool-descriptions-are-prompts.html`), confirming the check actually catches this class of regression rather than just looking plausible.
- Docs updated: `docs/technical-debt/backlog.md` (item 2 hardened-note, item 6 cross-referenced as a first concrete verify-only-CI instance, item 10 updated to note the one narrow structural check that now exists), `docs/seo/checklist.md` (sitemap row + maintenance note updated, page count 16→17), `docs/testing/strategy.md` (documented the first CI-enforced check in the repo), `docs/roadmap/roadmap.md` (review date + summary, no changes to the unaffected Immediate items), `docs/architecture/overview.md` (article count 7→8, repository layout, build & deploy pipeline section).
- Follow-ups: None new. The larger open items are unchanged and still queued: real per-page SEO meta tags (backlog #1), Trends Board heading hierarchy (#4), wire `site/` into CI/deploy or decide not to (#8), fate of the 8 published legacy articles (#9), and the broader legacy-page tooling ADR (#6) — this run's CI check is explicitly scoped narrower than that ADR and doesn't preempt it.

## 2026-07-25
- Task: Root-caused a sitemap-staleness regression that had recurred a third time. Replaced the hand-maintained `sitemap.xml` with `scripts/generate-sitemap.sh`, which derives every entry (URL, priority, changefreq, `<lastmod>` from each page's own git history) from the published pages on disk. `scripts/check-sitemap.sh` now diffs the committed file against the generator's output instead of only checking for missing entries. `.github/workflows/deploy-pages.yml` runs the generator immediately before uploading the Pages artifact, so the live sitemap can never be stale regardless of what's committed — and the step cannot fail the deploy, since it only ever regenerates. Also restarted this run's designated branch from `origin/main`: it had been seeded from the wrong parent (`claude/daily-ai-trends-tutorial-txft7z`, a separate content-authoring lineage), diverged from `main` with no shared ancestry, and carried no open PR — continuing on it would have produced a PR full of unrelated commits.
- Category: Failing/broken CI (priority 1) — this pre-empted the SEO-meta-tags task originally selected during orientation, per the operating instructions' "any health-check failure becomes today's task" rule.
- Why: STEP 2 (verify health) ran `scripts/check-sitemap.sh` against `main` and it failed: today's new article (`articles/mcp-anatomy-hosts-clients-servers-primitives.html`, published and linked, but absent from `sitemap.xml`) — the exact regression class the 2026-07-22 CI check was built to catch, recurring for a third time despite that check already existing. Investigating further (comparing every entry's `<lastmod>` against `git log`) found a second, previously invisible failure mode: `context-compaction-for-long-running-agents.html`'s entry was one day stale, which the missing-entries-only check never covered. Two prior fixes (a prose reminder, then a verify-only "missing entries" CI check) both assumed a human or agent would keep the file accurate by hand and both failed for different reasons. Generating the file mechanically — and regenerating it again at deploy time regardless of what's committed — removes the dependency on anyone remembering, rather than adding a fourth reminder that would likely fail the same way.
- Files: `sitemap.xml` (regenerated), `scripts/generate-sitemap.sh` (new), `scripts/check-sitemap.sh` (rewritten to diff-based), `.github/workflows/deploy-pages.yml` (added a "Regenerate sitemap.xml" step before the Pages upload, `fetch-depth: 0` on checkout so `git log` per-file works), `.github/workflows/check-sitemap.yml` (`fetch-depth: 0` for the same reason), `docs/seo/checklist.md`, `docs/technical-debt/backlog.md`, `docs/roadmap/roadmap.md`, `docs/testing/strategy.md`, `docs/architecture/overview.md`.
- Verification: `./scripts/check-sitemap.sh` passes against the regenerated file; confirmed idempotent (running the generator twice produces byte-identical output); `python3 -c "import xml.dom.minidom as m; m.parse('sitemap.xml')"` confirms well-formed XML; both modified workflow YAML files parsed successfully with `yaml.safe_load`; `site/`'s `npm run build` and `npx astro check` both still pass clean (0 errors/warnings/hints), confirming the Astro pilot subtree is unaffected. Could not execute the GitHub Actions workflows themselves in this environment (no CI runner access) — reasoned through the `fetch-depth: 0` requirement instead of observing it directly; flagged as a follow-up to watch on the next real push to `main`.
- Docs updated: `docs/seo/checklist.md` (sitemap row + maintenance note rewritten), `docs/technical-debt/backlog.md` (item 1 cross-referenced against open PR #11 to prevent duplicate future work; item 2 rewritten with the full three-recurrence history and root-cause fix), `docs/roadmap/roadmap.md` (review date, Immediate item 1 narrowed and cross-referenced to PR #11), `docs/testing/strategy.md` (current-state section rewritten), `docs/architecture/overview.md` (repository layout, build & deploy pipeline section, review date).
- Follow-ups: (1) Watch the next real push to `main` to confirm `deploy-pages.yml`'s new "Regenerate sitemap.xml" step actually runs correctly end-to-end in GitHub's runner (verified locally and by code review only, not by observing an actual Actions run). (2) PR #11 (open since 2026-07-20, targets `main`) already proposes real SEO meta tags for `articles/*.html` — flagged in the backlog and roadmap so a future run doesn't duplicate that slice; if it merges first, the next SEO-meta-tags run should scope to `index.html` + `manual/*.html` only. (3) Noticed but did not act on: PR #16 ("Add robots.txt and sitemap.xml for the legacy site", open since 2026-07-23) targets `claude/daily-ai-trends-tutorial-txft7z` instead of `main` and duplicates already-shipped work (robots.txt/sitemap.xml have existed on `main` since 2026-07-18) — likely the same wrong-base-branch issue this run hit and fixed for itself; worth a human or future run closing it as stale, but out of scope for this run to touch another branch's open PR.

## 2026-07-26
- Task: Root-caused a *fourth* recurrence of the sitemap-staleness regression. `check-sitemap.yml` was found failing on `main` (commit `616b950`, PR #19's merge — `articles/memory-taxonomies-for-agents.html` published without the committed `sitemap.xml` being regenerated). Regenerated and committed `sitemap.xml` to restore a green check, and fixed the actual root cause: `SCHEDULED_TASK_PROMPT.md` (the separate daily content-authoring agent's operating spec) had never been updated when the 2026-07-25 run replaced the hand-maintained sitemap with a generator — its "Finish every run" step 1a still told that agent to hand-add a `<url>` entry itself. Rewrote step 1a to call `./scripts/generate-sitemap.sh` and state plainly that the file must never be hand-edited.
- Category: Failing/broken CI (priority 1) — STEP 2 (verify health) found `check-sitemap.sh` failing against the current working tree before any task selection; per operating instructions this pre-empted any other candidate task.
- Why: This run's designated branch (`claude/stoic-allen-iyfns2`) had no commits of its own and no open PR (confirmed via `list_pull_requests` with `head:tseten1996:claude/stoic-allen-iyfns2` → empty), and diffing it against `origin/main` showed it was purely stale (zero branch-only additions) — restarted from `origin/main` per the already-merged-branch protocol before orienting. Orientation's mandatory health check then found `check-sitemap.sh` failing locally, and `mcp__github__actions_list` confirmed the same workflow was actually red on `main` at HEAD (run `30210031178`, conclusion `failure`) — a live, current CI failure, not a hypothetical one. Three prior fixes (a prose reminder, a missing-entries-only CI check, then a full generator+diff-check rewrite) had each addressed the *artifact* or the *CI tooling* but never checked whether the downstream instruction file that actually produces commits (`SCHEDULED_TASK_PROMPT.md`) was consistent with the new tooling — it wasn't, which is why the fix held for exactly one cycle (PR #18, which happened to regenerate correctly) and then broke again on the very next content commit (PR #19).
- Files: `sitemap.xml` (regenerated), `SCHEDULED_TASK_PROMPT.md` (step 1a rewritten), `docs/technical-debt/backlog.md`, `docs/seo/checklist.md`, `docs/roadmap/roadmap.md`, `docs/architecture/overview.md`.
- Verification: `./scripts/check-sitemap.sh` passes against the regenerated `sitemap.xml` (was failing before the fix, confirmed via direct diff output — a missing `<url>` entry for `articles/memory-taxonomies-for-agents.html` plus two stale `<lastmod>` dates). `python3 -c "import xml.dom.minidom as m; m.parse('sitemap.xml')"` confirms well-formed XML. `site/`'s `npm install && npm run build` (2 pages, 0 errors) and `npx astro check` (0 errors/0 warnings/0 hints) both still pass clean, confirming the Astro pilot subtree is unaffected. No other build/lint/test pipeline exists in this repo for the legacy pages (unchanged from prior audits).
- Docs updated: `docs/technical-debt/backlog.md` (item 2 — full fourth-recurrence history and root cause), `docs/seo/checklist.md` (maintenance note rewritten), `docs/roadmap/roadmap.md` (review summary), `docs/architecture/overview.md` (review date).
- Follow-ups: None new. Watch whether `check-sitemap.yml` stays green on `main` through the next several content-authoring runs now that `SCHEDULED_TASK_PROMPT.md` matches the actual tooling — if it recurs a fifth time despite the instruction fix, the content-authoring agent itself (not just its spec) would need investigating. PR #11 (SEO meta tags, open since 2026-07-20) and PR #16 (stale, wrong-base-branch, open since 2026-07-23) are both still open and unchanged from the prior run's notes — not touched this run, out of scope.
